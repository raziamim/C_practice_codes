#include <stdio.h>
#include <stdlib.h>

int main()
{
    int firstInteger,secondInteger;
    printf("Input the first integer:");
    scanf("%d",&firstInteger);
    printf("Input the second integer:");
    scanf("%d",&secondInteger);
    printf("Sum of the above two integer=%d",firstInteger+secondInteger);
    return 0;
}
